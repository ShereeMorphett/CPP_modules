#include  <string.h> 
#include <iostream>
 
namespace ft
{
    /// used for practice testing
    void hello_world();
    int add(int a, int b);
	/// used for megaphone

	/// used for phonebook
	bool isValidPhonenumber(std::string str);
	std::string		TrimWs(std::string input);
}